import { defineConfig } from 'astro/config';

export default defineConfig({
  // Change this to your real domain once it's bought (used for RSS + share links)
  site: 'https://prettyballersports.com',
});
